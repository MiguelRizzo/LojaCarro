@model IEnumerable<WebAppautomovel3.Models.Item>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorUnitario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Automovel.Modelo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Automovel.Ano)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Automovel.Foto)
        </th>
        <th>
            Subtotal
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorUnitario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Automovel.Modelo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Automovel.Ano)
        </td>
        <td>
           <img src="@item.Automovel.Foto" class="img-responsive" style="height:40px;">
        </td>
        <td>
            @string.Format("{0:c}",item.Quantidade*item.ValorUnitario) 
        </td>
        <td>
            
        </td>
    </tr>
}

    <tr>
        <td colspan="6">TOTAL COMPRA </td> <td>@string.Format("{0:c}", ViewBag.Total) </td>
    </tr>

</table>
